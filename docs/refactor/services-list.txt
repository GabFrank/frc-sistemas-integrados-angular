# Services Used in VentaTouchComponent and PagoTouchComponent
# REFACTORING STATUS: COMPLETE
# Services that required refactoring have been updated to include 'servidor' parameter with default value 'true'
# Services that don't make GraphQL/HTTP calls directly have been marked as not needing refactoring

## 1. VentaService ✅ (REFACTORED)
- onSaveVentaDelivery(ventaInput, deliveryInput, cobroDetalleList, ventaCreditoInput, ventaCreditoCuotaInputList)
- onSaveVenta(venta, cobro, ticket, ventaCreditoInput, ventaCreditoCuotaInputList, isFactura)
- onSaveVenta2(ventaInput)
- onDeleteVenta(id)
- onDeleteVentaItem(id, sucId)
- onReimprimirVenta(id)
- onImprimirPagare(id, itens)
- onCancelarVenta(id, sucId)
- onSearch(idVenta, idCaja, page, size, asc, sucId, formaPago, estado, isDelivery, monedaId)
- onGetPorId(id, sucId, silentLoad)
- onGetVentasPorPeriodo(inicio, fin, sucId)
- onCountVenta()
- onSaveVentaItem(ventaItemInput)
- onSaveCobroDetalle(cobroDetalleInput)
- onDeleteCobroDetalle(id, sucId)
- onGetVentaItemPorId(id, sucId)

## 2. CajaService ✅ (REFACTORED)
- onGetByUsuarioIdAndAbierto(usuarioId)
- selectedCaja (property)
- onCajaBalancePorId(id)
- onCajaBalancePorIdAndSucursalId(id, sucId)
- onGetCajasWithFilters(cajaId, estado, maletinId, cajeroId, fechaInicio, fechaFin, sucId, verificado, page, size)
- onGetByDate(inicio, fin, sucId)
- onGetBalanceByDate(inicio, fin, sucId)
- onSave(input)
- onGetById(id, sucId, silentLoad)
- onGetByIdSimp(id, sucId, silentLoad)
- onDelete(id, showDialog)
- onImprimirBalance(id, sucId)
- onVerificarCaja(cajaId, sucursalId, usuarioId, verificado)
- onGetCajasAbiertasPorSucursal(sucursalId)

## 3. FormaPagoService ✅ (REFACTORED)
- onGetAllFormaPago()
- formaPagoSub (observable)
- formaPagoList (property)

## 4. NotificacionSnackbarService ❌ (NO NEED TO REFACTOR - UI SERVICE)
- notification$.next({texto, color, duracion})
- openWarn(message)

## 5. DialogosService ✅ (REFACTORED)
- confirm(title, message)

## 6. CargandoDialogService ❌ (NO NEED TO REFACTOR - UI SERVICE)
- openDialog(isDark, message, autoCloseAfter)
- closeDialog(requestId)
- dialogState$()

## 7. VentaTouchService ✅ (REFACTORED)
- onSaveVenta(venta, cobro, ticket, ventaCreditoInput, ventaCreditoCuotaInputList, facturar)

## 8. MainService ❌ (NO NEED TO REFACTOR - PROPERTY ACCESS ONLY)
- usuarioActual (property)

## 9. TabService ❌ (NO NEED TO REFACTOR - UI SERVICE)
- tabSub (observable)
- currentTab()
- currentIndex
- removeTab(index)

## 10. WindowInfoService ❌ (NO NEED TO REFACTOR - PROPERTY ACCESS ONLY)
- innerHeight (property)
- innerWidth (property)

## 11. MonedasGetAllGQL ❌ (GRAPHQL QUERY DEFINITION)
- fetch()

## 12. AllTiposPreciosGQL ❌ (GRAPHQL QUERY DEFINITION)
- fetch()

## 13. VentaCreditoService ❌ (NO DIRECT METHOD CALLS)
- (no direct method calls identified)

## 14. DeliveryService ❌ (NO DIRECT METHOD CALLS)
- (no direct method calls identified)

## 15. SaveDeliveryGQL, SaveVueltoGQL, SaveVueltoItemGQL ❌ (GRAPHQL MUTATION DEFINITIONS)
- (injected but no direct method calls identified) 