import { AutoFitTextDirectiveDirective } from './auto-fit-text-directive.directive';

describe('AutoFitTextDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFitTextDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
