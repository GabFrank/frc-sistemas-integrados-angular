import { FormattedTooltipDirective } from './formatted-tooltip.directive';

describe('FormattedTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new FormattedTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
