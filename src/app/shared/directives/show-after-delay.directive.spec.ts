import { ShowAfterDelayDirective } from './show-after-delay.directive';

describe('ShowAfterDelayDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAfterDelayDirective();
    expect(directive).toBeTruthy();
  });
});
