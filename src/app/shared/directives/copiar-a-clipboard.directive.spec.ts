import { CopiarAClipboardDirective } from './copiar-a-clipboard.directive';

describe('CopiarAClipboardDirective', () => {
  it('should create an instance', () => {
    const directive = new CopiarAClipboardDirective();
    expect(directive).toBeTruthy();
  });
});
